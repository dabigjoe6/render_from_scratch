
#ifndef INCLUDE_DEFS_HPP
#define INCLUDE_DEFS_HPP

#include <ncurses.h>

#define PI (3.141592653f)

#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) > (b) ? (b) : (a))

#define WW (COLS)
#define WH (LINES)

#endif
